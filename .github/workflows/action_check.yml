name: 'Action Test'
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - 'main'
    tags-ignore:
      - '**'
    paths:
      - ".github/workflows/action_check.yml"
      - "action.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      LOG_LEVEL_VERBOSE: true
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Test Everything
        uses: ./
        with:
          api_token: ${{ secrets.TEST_TOKEN }}
          zone: ${{ secrets.TEST_ZONE }}

      - name: Test URL List
        uses: ./
        with:
          api_token: ${{ secrets.TEST_TOKEN }}
          zone: ${{ secrets.TEST_ZONE }}
          URLs: |
            https://azure.community.cyberjake.xyz/
            https://azure.community.cyberjake.xyz/favicon.ico

      - name: Test URL List
        uses: ./
        with:
          api_token: ${{ secrets.TEST_ACCOUNT_TOKEN }}
          zone: ${{ secrets.TEST_ZONE }}
          URLs: |
            https://azure.community.cyberjake.xyz/
            https://azure.community.cyberjake.xyz/favicon.ico

